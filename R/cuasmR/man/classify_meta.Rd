% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bench_meta.R
\name{classify_meta}
\alias{classify_meta}
\title{Decide whether a measurement is comparable to a baseline.}
\usage{
classify_meta(pre, post, valid_when = list())
}
\arguments{
\item{pre, post}{\code{\link{capture_gpu_state}()} snapshots taken
before / after the bench.}

\item{valid_when}{Optional list with any of:
\describe{
  \item{require_no_throttle}{logical (default TRUE)}
  \item{allow_throttle}{character vector of names that ARE OK
    (default \code{"GpuIdle"})}
  \item{min_clock_sm}{numeric MHz floor}
  \item{max_temp_c}{numeric ceiling}
  \item{require_ac}{logical (laptop)}
}}
}
\value{
\code{list(ok, reasons, summary)}. \code{ok} is \code{NA} if
  \code{pre} or \code{post} is \code{NULL} (no GPU detected).
}
\description{
Decide whether a measurement is comparable to a baseline.
}
