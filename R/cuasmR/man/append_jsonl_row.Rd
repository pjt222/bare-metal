% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bench_io.R
\name{append_jsonl_row}
\alias{append_jsonl_row}
\title{Append one row to a JSONL file as a single line.}
\usage{
append_jsonl_row(jsonl_path, row)
}
\arguments{
\item{jsonl_path}{Path to the JSONL file (created/extended).}

\item{row}{A named list serialised with \code{jsonlite::toJSON}
(\code{auto_unbox = TRUE}, \code{na/null = "null"}, full precision).}
}
\value{
Invisibly \code{NULL}.
}
\description{
\code{cat(..., append = TRUE)} is atomic at line boundaries on POSIX
(and on Windows for short writes below the pipe/page size), which is
what makes the store crash-safe: a hard kill can only corrupt the
final line, never an earlier row.
}
